# Test-Claude

## Projects in this repo

- `SOI POS/` — SOI Threading Salon point-of-sale app (TanStack Start + Supabase).
  Imported from Lovable and made standalone; see `SOI POS/README.md`.

## Commands from the user

- When the user says **"run"** (alone, or "run it", "run the app", "run SOI POS"),
  start the SOI POS dev server locally by following `.claude/skills/run-soi-pos/SKILL.md`,
  then report the URL and whether the gate page responds. Do not ask which project.
